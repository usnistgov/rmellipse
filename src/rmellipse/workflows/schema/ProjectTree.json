{
  "title":"Schema for mapping a project structure.",
  "type":"object",
  "properties":
  {
    "leafs":{
      "type":"object",
      "additionalProperties":{
        "type":"object",
        "properties":{
          "dataset_type":{
            "type":"string",
            "description":"describes if the file is intended to be a dataset for release on CDCS, or part of the source code.",
            "enum":["workflow","dataset","source"]
          },
          "id":{
            "type":"string",
            "description":"CDCS Object ID"
          },
          "persistent_id":{
            "type":"string",
            "description":"CDCS Object ID"
          },
          "version":{
            "type":"string",
            "description":"Version of this particular dataset (match semantic versioning)"
          },
          "project":{
            "type":"string",
            "description":"Name of the project this belongs too."
          }
        }
      },
      "required:":["is_dataset","version","project"]
    }
  }
}